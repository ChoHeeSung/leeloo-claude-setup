---
description: "공문서(HWP/HWPX/PDF) → 마크다운 변환"
argument-hint: "<file> [--pages <range>] [--json] [--metadata] [--table <N>]"
---
`lk-doc-parse` 스킬을 실행합니다. 사용자 입력: $ARGUMENTS

서브커맨드 파싱과 대화형 흐름을 포함한 동작 정의는 대응 SKILL.md(`lk-doc-parse`)를 기준으로 합니다.
